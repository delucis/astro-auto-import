# Demo Site

This is a demo Astro project to test the `astro-auto-import` integration with.

`cd` into this directory and run `pnpm start` to run the project with a development server or `pnpm run build` to build it.